
const mailService = {
    addMoreSlotsHosts: async (host, hosts, organizer, guest) => null,

    askForMoreSlots: async (guest, hosts) => null,

    pickSlotGuest: async (guest, meetingUuid, organizer) => null
}


exports.MailService = mailService